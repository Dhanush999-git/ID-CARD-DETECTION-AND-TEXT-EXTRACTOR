MINI PROJECT 
steps to install and run:
1. install py -3.10
2. install and activate venv
3. pip3 install -r requirements-cpu.txt
   or
3. i) pip3 install tensorflow==2.17.1
  ii) pip3 install -r requirements-cpu.txt
4. 
